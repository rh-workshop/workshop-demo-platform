# En prod el emisor de cara al exterior es ACME/CA corporativa (acme-production);
# la CA propia del taller queda para trafico interno. Ambos issuers van en la base.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
