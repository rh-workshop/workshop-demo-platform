# Rango de IPs de PROD, distinto del de dev (evita conflicto ARP si comparten L2).
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: workshop-pool
  namespace: metallb-system
spec:
  addresses:
    - 10.10.20.200-10.10.20.220
