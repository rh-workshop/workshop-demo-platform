apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # NooBaa antes que el QuayRegistry: el objectstorage 'managed' depende de su API.
  - noobaa.yaml
  # Backing store y bucket class por defecto: con manualDefaultBackingStore el operador no los crea solo.
  - backingstore.yaml
  - quayregistry.yaml
