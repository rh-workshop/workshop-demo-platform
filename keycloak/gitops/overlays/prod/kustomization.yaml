apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: keycloak
resources:
  - ../../base
  - keycloak-pdb.yaml
patches:
  - path: keycloak-prod.yaml
  - target:
      kind: Route
      name: keycloak
    patch: |-
      - op: replace
        path: /spec/host
        # Sustituir por el dominio del cluster de PRODUCCIÓN: sso.apps.<cluster-prod>.<dominio-base>
        value: sso.apps.ocp.k6ln6.sandbox1361.opentlc.com
labels:
  - pairs:
      app.kubernetes.io/part-of: workshop-platform
      workshop.redhat.com/environment: prod
