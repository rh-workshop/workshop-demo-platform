# El hostname es lo unico que cambia entre ambientes: se parchea aqui.
apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
  namespace: keycloak
spec:
  hostname:
    hostname: sso.apps.<dominio-del-cluster>
