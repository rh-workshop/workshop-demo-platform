apiVersion: kuadrant.io/v1
kind: AuthPolicy
metadata:
  name: shared-gateway-default
  namespace: platform-gateway
spec:
  defaults:
    rules:
      authentication:
        token-valido:
          jwt:
            # Dominio del cluster HUB (un overlay por CLUSTER: el hostname es literal y dos clusters no pueden compartirlo).
            issuerUrl: https://sso.apps.ocp.4t8cv.sandbox3572.opentlc.com/realms/workshop
