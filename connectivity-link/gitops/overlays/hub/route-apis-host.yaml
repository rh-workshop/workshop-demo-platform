# Host compartido de APIs del cluster HUB.
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: shared-gateway-apis
  namespace: platform-gateway
spec:
  # Dominio del cluster HUB (un overlay por CLUSTER: el hostname es literal).
  host: apis.apps.ocp.hq2ss.sandbox3270.opentlc.com
