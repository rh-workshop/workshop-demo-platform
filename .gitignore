# Nunca versionar credenciales ni ficheros de entorno
.env
*.env
vault-password*
*-secret.yaml
*.key
*.pem

# Inventario real de clusters del bootstrap: nombra clusters reales, nunca a Git
bootstrap/ansible/clusters.yml
