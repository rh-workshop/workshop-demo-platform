# Aplica la Policy en el HUB. El hub tambien aloja namespaces de negocio
# mientras el CI corra aqui, y ademas es donde vive el secreto de origen: sin
# este binding la credencial existiria pero nadie la replicaria localmente.
apiVersion: policy.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  annotations:
    # El CRD lo trae el propio MCH de este sync: sin esto, el dry-run del arranque en frio invalida el sync entero de la app acm.
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
  name: require-registry-pull-secret-binding
  namespace: open-cluster-management
placementRef:
  apiGroup: cluster.open-cluster-management.io
  kind: Placement
  name: clusters-hub
subjects:
  - apiGroup: policy.open-cluster-management.io
    kind: Policy
    name: require-registry-pull-secret
