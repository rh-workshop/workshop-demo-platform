apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Las cinco credenciales van en un solo fichero: comparten proposito, formato y
  # ciclo de vida, y separarlas en cinco copias casi identicas solo multiplica el
  # sitio donde equivocarse al cambiar el patron.
  - externalsecret.yaml
